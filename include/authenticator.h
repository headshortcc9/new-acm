#include"storage_manager.h"
#include<vector>
class authenticator
{
private:
    /* data */
public:
    authenticator(/* args */);
    ~authenticator();
    void vectorRemoveDuplicateItem(std::vector<std::string> &v);
    void print_recommend(std::vector<std::string> reds);
    void get_authenticator();
};

